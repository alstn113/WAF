import * as S from './ShortAnswerFom.styles';
interface Props {
  index: number;
}

const ShortAnswerForm = ({ index }: Props) => {
  return <S.Text>단답형 텍스트</S.Text>;
};

export default ShortAnswerForm;
